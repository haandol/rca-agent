{
  "schemaVersion": 2,
  "scenarioId": "deployment-query-regression",
  "engine": "strands",
  "rootCause": "The deployment introduced an N+1 query loop in the sensor response path.",
  "rootCauseConfirmed": true,
  "rootFaultType": "slow-query",
  "rootCauseEvidenceIds": [
    "deployment-correlation",
    "query-volume-spike",
    "repository-diff"
  ],
  "evidenceIds": [
    "deployment-correlation",
    "query-volume-spike",
    "repository-diff"
  ],
  "competingCauseJudgments": [],
  "artifacts": ["scoping", "hypotheses", "validation", "report", "playbook"],
  "remediation": {
    "summary": "Rollback the deployment, then replace the per-sensor query with a batched query.",
    "available": true,
    "verificationStatus": "DRAFT",
    "executionSteps": [
      {
        "stepId": "restore-query-path",
        "intent": "Restore the last healthy query behavior.",
        "action": "Roll back the affected service deployment to the last healthy revision.",
        "successCriteria": "Query volume per request and p99 latency return to their healthy ranges."
      }
    ],
    "safe": true,
    "unsafeSteps": [],
    "safeguards": {
      "preconditions": "Confirm the latest deployment introduced the query regression and identify the last healthy revision.",
      "approval": "Require the service owner or on-call SRE to approve the rollback.",
      "rollback": "Stop or reverse the change if error rate or query latency increases.",
      "verification": "Verify query volume, latency, and error rate across the configured observation window."
    }
  }
}
