# Decision Log: infra

이 문서는 infra 카테고리의 주요 결정 변경 이력이다. 각 ADR 본문은 현재 상태만
서술하고, 개별 diff는 Git이 보존한다.

| 날짜 | 변경 | 근거 | 현재 ADR |
|------|------|------|----------|
| 2026-07-31 | 커넥션 알람 임계치를 30에서 12로 낮추고 명시적 누수 주입 기본값을 10에서 20으로 올려, 정상 사용량 < 임계치 < 풀 고갈 순서를 계약으로 고정 | 임계치가 애플리케이션 풀 고갈 지점(15)보다 높아 누수가 풀을 다 써도 원인 지표가 침묵했고, 기본 주입량이 풀 용량보다 적어 단일 주입으로 증상에 도달하지 못했으므로 | [데모 시나리오](0007-demo-symptom-alarm-and-deployment-fault-injection.md) |
| 2026-07-31 | 증상 알람 진입점 보장을 커넥션 누수 시나리오로 한정하고, 자원 사용률·질의 지연은 검증용 원인 지표로 위치를 명시 | 자원 사용률과 질의 지연은 바이탈 수집을 실패시키지 않아 증상을 만들려면 인위적 실패 경로가 필요하고, 그러면 인과 사슬이 시연을 위한 조작이 되므로 | [데모 시나리오](0007-demo-symptom-alarm-and-deployment-fault-injection.md) |
| 2026-07-31 | 사람이 시작하는 취소를 상태 변경에서 claim 회전으로 바꾸고, 삭제를 최종 상태·lease 없음 조건으로 제한 | 상태만 바꾸는 취소는 claim을 든 실행의 늦은 쓰기를 막지 못하고, 활성 세션의 삭제는 중복 실행을 막는 fencing 기준 자체를 제거하므로 | [세션 복구](0006-session-recovery-on-restart.md) |
| 2026-07-31 | 알람 입력 토픽의 발행을 서비스 주체 허용에서 이 배포의 계정·알람 출처를 함께 요구하는 조건으로 좁힘 | 서비스 주체만 허용하면 모든 계정의 알람이 두 엔진의 전체 실행을 기동할 수 있고, 알람 수신은 인증 없는 진입점이므로 | [알람 수신 아키텍처](0001-alarm-ingestion-sns-sqs-fargate.md) |
| 2026-07-30 | 자동 복구 워커 스택을 제거하고 사용자 요청 큐를 진입점으로 하는 실행 전용 스택을 신설 | 이벤트 구독은 승인 없이 실행을 기동하는 경로이므로 승인 게이트를 코드 조건이 아니라 인프라 경계로 세우기 위해 | [플레이북 실행 인프라](0008-playbook-execution-stack.md) |
| 2026-07-30 | 분석 태스크 역할에서 모든 변경 권한과 대상 서비스 네트워크 경로를 제거 | 쓰기 권한을 실행 스택의 태스크 역할 하나에 격리해 분석 경로의 결함이 서비스 변경에 닿지 않게 하기 위해 | [CC Headless 실행 인프라](0003-lambda-cc-headless-stack.md), [플레이북 실행 인프라](0008-playbook-execution-stack.md) |
| 2026-07-30 | 실행 증거를 오브젝트 저장소 주 보관 + 상태 저장소 요약으로 분리하고 갱신 전 플레이북 사본을 보존 | 명령 단위 증거는 크기가 예측되지 않아 항목 한도에 걸리고, 갱신이 원본을 덮어써 diff 기준을 사후 복원할 수 없으므로 | [증거 저장](0002-evidence-storage.md) |
| 2026-07-30 | 실행 시도를 엔진과 무관한 실행 단위 trace 항목으로 기록 | 실행 경로가 엔진과 무관하게 하나이고, 같은 리포트의 재실행이 서로 덮어쓰지 않아야 하므로 | [실행 trace](0005-execution-trace-dynamodb.md) |
| 2026-07-25 | 하네스와 에이전트 런타임을 실행 시점 외부 조회에서 이미지 사전 동봉·버전 고정으로 전환 | 이미지 태그 하나로 로컬 검증 구성을 재현하고 외부 저장소 장애가 RCA 실행을 깨뜨리지 않게 하기 위해 | [CC Headless 실행 인프라](0003-lambda-cc-headless-stack.md) |
| 2026-07-21 | 세션 reclaim 세대를 SQS 메시지 식별자와 해당 메시지의 receive count 조합으로 한정 | 별도 중복 메시지의 독립적인 receive count가 정상 실행의 claim을 탈취하지 못하게 하기 위해 | [세션 복구](0006-session-recovery-on-restart.md) |
| 2026-07-21 | 세션 claim fencing을 상태 전이에서 외부 부작용 시작 경계까지 확장 | reclaim된 이전 실행의 reset·보고서·알림·trace 쓰기를 차단하기 위해 | [세션 복구](0006-session-recovery-on-restart.md) |
| 2026-07-21 | 미완료 CC 세션 복구를 단순 중복 확인에서 SQS receive count와 claim token 기반 원자적 reclaim으로 강화 | 재전달을 실제 재실행으로 연결하면서 이전 실행의 늦은 상태·결과 확정을 차단하기 위해 | [세션 복구](0006-session-recovery-on-restart.md) |
| 2026-07-21 | CC Headless 전용 외부 복구 홉 대신 동일 Fargate 실행 안의 제한된 복구 도구를 채택 | 분석 컨텍스트를 유지하고 복구 지연을 줄이면서 일반 인프라 쓰기 권한을 제거하기 위해 | [CC Headless 실행 인프라](0003-lambda-cc-headless-stack.md) |
