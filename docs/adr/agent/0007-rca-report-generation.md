# ADR 0007: RCA 보고서 생성 — LLM 기반 자동 보고서 작성

Date: 2026-04-21
Updated: 2026-08-08

## Status

Accepted (2026-08-08)

## Context

근본 원인이 확정(또는 미확정)된 후 SRE 팀이 활용할 수 있는 종합 RCA 보고서를 자동으로 생성해야 한다. 수동 보고서 작성은 수시간이 소요되며, 에이전트가 이미 수집한 증거와 추론 경로를 구조화하면 작성 시간을 크게 단축할 수 있다.

분석의 산출물이 보고서와 플레이북 두 문서로 나뉘어 서로 다른 저장소에 놓이면, 사용자가 "이 장애의 분석 결과"로 열어볼 단위가 없다. 특히 플레이북이 실행 근거가 되면서([ADR 0008](0008-playbook-generation.md)) 사용자는 원인 판단과 실행 절차를 함께 보고 실행을 승인해야 한다. 두 문서를 별도로 조회하게 하면 승인 판단에 필요한 맥락이 갈라진다.

또한 실행이 분석과 다른 생명주기를 갖게 되면서([ADR 0017](0017-playbook-execution-agent.md)) 보고서가 복구 결과를 본문에 품는 구조가 성립하지 않는다. 보고서는 분석이 끝난 시점에 확정되고, 실행은 그 뒤 사용자 승인으로 여러 번 시도될 수 있다.

## Decision Drivers

- 보고서는 확정과 미확정을 명확히 구분해야 한다. SRE가 미확정 결과를 확정으로 오독하면 잘못된 조치가 시작된다.
- 보고서 생성 실패가 RCA 결과 자체를 잃게 만들어서는 안 된다.
- 인과 사슬은 "운영자 실수" 같은 사람 지목에서 멈추지 않고 시스템·프로세스 결함까지 내려가야 한다. 사람에서 멈춘 분석은 재발 방지 조치를 만들 수 없다.
- 보고서의 각 주장은 수집된 증거나 타임라인 이벤트에 연결되어야 한다.
- 사용자가 실행을 승인하려면 원인 판단과 실행 절차를 한 문서에서 볼 수 있어야 한다.
- 보고서는 분석 시점에 확정되며, 이후 실행 결과가 보고서를 변경해서는 안 된다.

## Decision

**LLM 기반 구조화 보고서 자동 생성** 전략을 채택한다.

### 보고서 구조

- 장애 요약 (알람 정보, 발생 시각, 영향 범위)
- 심각도 (서비스 영향 범위와 지속 시간 기반: critical/high/medium/low)
- 영향 평가 (영향받은 서비스, 사용자 범위, 지속 시간, 에러율/지연 등 정량적 영향)
- 탐지 방법 (어떤 알람/메트릭/모니터링이 인시던트를 감지했는지)
- 근본 원인 (확정된 가설, 신뢰도)
- **5 Whys** (증상 → 시스템·프로세스 결함까지의 인과 사슬 — Toyota / AWS COE 가드레일 적용)
- 가설 도출 경로 (트리에서 근본 원인까지의 경로, 기각된 가설 목록)
- 증거 목록 (메트릭 스냅샷, 로그 스니펫, 배포 이력, 코드 diff 참조)
- 임시 조치 방안 (예: 서비스 롤백, 리소스 증설)
- 영구 조치 방안 (예: 코드 수정, 설정 변경)
- 조치 항목 (재발 방지, 영향 축소, 프로세스 개선으로 분류된 후속 작업)
- 교훈 (탐지/대응에서 잘된 점, 개선할 점, 운이 좋았던 점)
- 타임라인 (알람 발생 → 스코핑 → 가설 생성 → 검증 → 확정까지 시간 흐름)
- **플레이북** (실행 절차를 포함한 대응 플레이북 — 아래 단일 산출물 참조)

### 단일 산출물

분석의 산출물은 **플레이북을 포함한 리포트 하나**다. 플레이북은 리포트의 한 섹션이며
별도 문서가 아니다. 사용자는 하나의 리포트에서 원인 판단과 실행 절차를 함께 읽고
실행을 승인한다.

플레이북은 유사도 검색과 회고 갱신의 대상이므로 구조화된 형태를 유지해야 한다
([ADR 0008](0008-playbook-generation.md)). 즉 리포트는 사람이 읽는 서술과 함께 그
플레이북의 구조화 표현을 보유하며, 둘은 같은 산출물의 두 표현이지 두 개의 산출물이
아니다. 서술과 구조가 어긋나면 사용자가 승인한 절차와 실행되는 절차가 달라지므로,
일치를 완료 시점에 검증한다.

**단, 플레이북 생성이 실패한 리포트는 절차 없이 확정된다.** 플레이북 생성 실패는
파이프라인을 중단시키지 않으므로([ADR 0008](0008-playbook-generation.md)) 리포트는
사람에게 전달되어야 하고, 이때 리포트 본문은 절차가 없음을 명시한다. 없는 절차를
암시하는 서술은 쓰지 않는다 — 사람이 승인할 절차가 있다고 오인하면 승인 화면에서
실행을 찾지 못하는 이유를 알 수 없다. 절차가 없는 리포트는 실행 대상이 아니므로 승인
게이트는 실행을 제공하지 않으며, 이는 절차 일치 검증을 우회하는 경로가 아니다.

### 핵심 결정사항

1. **LLM 구조화 출력 + 오케스트레이션 메타데이터 결합**: 위 섹션 목록을 구조화 출력으로 받고, 오케스트레이션 계층이 수집한 메타데이터(RCA 식별자, 가설 경로, 증거 목록, 기각된 가설)를 덧붙여 최종 보고서를 구성한다. 탐색 과정에서 실제로 일어난 일은 모델이 아니라 오케스트레이터가 알고 있으므로, 이 부분을 모델 출력에 의존하지 않는다.

2. **Markdown 저장**: 보고서를 Markdown으로 렌더링해 오브젝트 스토리지에 보관한다. 저장 대상이 설정되지 않은 환경에서는 업로드를 건너뛰고 파이프라인을 계속 진행한다.

3. **근본 원인 미확정 처리**: 근본 원인이 확정되지 않은 보고서는 본문에 미확정임을 명시하고 해당 원인을 "가장 유력한 후보"로 표기한다.

4. **타임아웃 및 fallback**: 보고서 생성에 타임아웃을 적용하고, 실패 시 알람 요약과 최적 가설만으로 최소 보고서를 생성한다. 보고서 생성 실패가 RCA 결과 전체의 손실이 되어서는 안 된다.

5. **모델 티어**: **Planning 티어**를 사용한다. 구조화된 보고서 작성에 깊은 추론이 필요하다([ADR agent/0010](0010-model-tier-architecture.md)).

6. **5 Whys 사고 프레임 (Toyota / AWS COE)**: 보고서의 5 Whys 섹션은 단순 양식이 아니라 증상 → 시스템·프로세스·설계 결함까지의 인과 사슬 추적이다. LLM 프롬프트에 다음 가드레일을 강제한다.
   - **"휴먼 에러" 종착 금지**: "운영자 실수" 같은 답에서 멈추지 않고 "왜 그게 가능했는가? (검증 부재? 권한 과다? 런북 결함?)" 까지 풀어 시스템·프로세스 결함으로 표현.
   - **단일 원인 가정 회피**: 다요인이 함께 작용한 경우 주축 사슬은 가장 부하가 큰 요인으로 잡되, 각 단계의 답에 공동 요인(co-factors)을 한 줄로 명시.
   - **사실 기반 절단**: 각 "왜?"의 답은 수집된 증거 또는 타임라인 이벤트와 연결되어야 하며, 추측이면 사슬을 거기서 자르고 "증거 부족" 으로 명시.
   - **Blameless 톤**: 사람·팀을 비난하지 않고 시스템 관점으로만 기술.

   가드레일은 가설 생성 단계에도 동일하게 적용된다([ADR agent/0002](0002-hypothesis-tree-lifecycle.md)). CC Headless 엔진도 시스템 프롬프트의 공통 원칙과 보고서 단계 프롬프트로 같은 가드레일을 주입한다([ADR agent/0011](0011-cc-headless-prompt-driven-rca.md)) — 두 엔진의 보고서가 같은 품질 기준으로 비교되어야 하기 때문이다.

7. **실행 결과는 보고서에 담지 않는다**: 보고서는 분석 시점에 확정되고, 실행은 그 뒤 사용자 승인으로 시작되어 여러 번 시도될 수 있다([ADR agent/0017](0017-playbook-execution-agent.md)). 실행 결과를 보고서 본문에 쓰면 확정된 문서를 사후에 변경해야 하고, 재실행 시 어느 시도의 결과인지 모호해진다. 실행 상태와 증거는 실행 생명주기가 보유하며 보고서는 이를 참조하지 않는다.

8. **플레이북이 초안임을 표기한다**: 실행되지 않은 플레이북은 검증되지 않은 절차다([ADR agent/0008](0008-playbook-generation.md)). 사용자가 실행을 승인할 때 그 절차가 아직 검증되지 않았음을 알아야 하므로, 리포트 서술은 플레이북이 초안임을 표기한다. 분석 시점의 플레이북은 예외 없이 초안이므로 이 표기는 리포트 본문에 고정된다.

   **단, 승인 화면이 보여주는 검증 상태는 리포트 본문이 아니라 플레이북의 현재 값이다.** 이전 실행의 회고가 절차를 검증됨으로 승격했을 수 있고([ADR agent/0018](0018-playbook-retrospective.md)), 승인 판단의 근거는 확정된 분석 시점이 아니라 지금 실행될 절차의 상태이기 때문이다. 리포트 본문이 분석 시점에 확정되는 것과 같은 이유로(결정사항 7), 이후 변하는 상태를 본문이 보유하지 않는다.

## 대안 검토

| 대안                                         | 장점                                                                              | 단점 및 미채택 이유                                                                                                                |
| -------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 템플릿 채우기(LLM 없이 필드 조립)            | 결정적이고 비용이 없으며 형식이 항상 일정하다.                                    | 증거 간 인과 관계와 5 Whys 사슬을 서술할 수 없어 SRE가 다시 해석해야 하고, 자동화의 이점이 사라진다.                               |
| 자유 서술 보고서(구조 없는 산문)             | 모델이 맥락에 맞게 유연하게 서술한다.                                             | 필수 항목 누락을 검증할 수 없고, 확정·미확정 구분이 서술에 묻힌다. 플레이북의 구조화 표현과 유사도 인덱싱의 입력으로도 쓸 수 없다. |
| 구조화 출력 + 오케스트레이션 메타데이터 결합 | 필수 항목을 강제하면서 서술 품질을 얻고, 탐색 사실은 모델 출력에 의존하지 않는다. | 스키마 유지 비용이 들고 모델 출력이 스키마를 만족하지 못하면 재시도가 필요하다.                                                    |

리포트와 플레이북의 관계에 대한 대안:

| 대안                                               | 장점                                                            | 단점 및 미채택 이유                                                                                                           |
| -------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 리포트와 플레이북을 별도 산출물로 유지 (이전 구조) | 각자 다른 저장소와 수명을 가질 수 있고 생성 단계가 독립적이다.  | 사용자가 실행을 승인할 때 원인 판단과 절차를 별도로 조회해야 해 승인 맥락이 갈라진다. "이 장애의 분석 결과"로 열 단위가 없다. |
| 플레이북을 리포트에 병합하고 구조화 표현을 없앰    | 문서가 하나이고 유지가 단순하다.                                | 유사도 검색·회고 갱신·실행이 모두 구조를 요구하므로, 서술만 남기면 세 소비처가 성립하지 않는다.                               |
| 플레이북을 포함한 단일 리포트 + 구조화 표현 병존   | 승인 맥락이 한 문서에 모이고 구조를 요구하는 소비처도 충족한다. | 서술과 구조가 어긋날 수 있어 일치 검증이 필요하다.                                                                            |

## Consequences

### Positive

- 수동 보고서 작성 시간(수시간)을 자동화로 크게 단축
- 에이전트의 추론 경로와 증거가 체계적으로 문서화
- 표준화된 구조로 보고서 품질 일관성 확보
- 5 Whys 가드레일로 재발 방지 조치를 도출할 수 있는 결함 수준까지 사슬이 내려간다
- 사용자가 하나의 리포트에서 원인 판단과 실행 절차를 함께 읽고 실행을 승인할 수 있다
- 보고서가 분석 시점에 확정되므로 재실행이 문서를 변경하지 않는다

### Negative

- LLM 생성 보고서의 문체가 조직 표준과 다를 수 있음
- 증거 요약 과정에서 핵심 정보가 누락될 수 있음
- 보고서 구조가 플레이북·유사도 인덱스의 입력이므로 섹션을 바꾸면 소비처를 함께 갱신해야 한다
- 서술과 구조화 표현이 같은 산출물에 병존하므로 둘의 일치를 검증해야 한다
- 실행 결과를 보고서에서 볼 수 없으므로 사용자가 실행 상태를 별도 경로로 조회해야 한다

### Risks

- 저장된 증거 조회에 실패하면 메타데이터만으로 보고서를 작성하게 되어 증거 세부 내용이 부족할 수 있다.
- 5 Whys의 각 단계가 증거에 연결되지 않으면 그럴듯한 추측 사슬이 만들어진다. 추측인 단계에서 사슬을 자르고 "증거 부족"으로 명시하도록 강제한다.
- 서술된 절차와 구조화된 절차가 어긋나면 사용자가 승인한 것과 실행되는 것이 달라진다. 완료 시점에 일치를 검증해 어긋난 리포트를 저장하지 않는다. 다만 절차 섹션을 모델이 쓰지 않고 실행 주체가 읽는 것과 같은 플레이북에서 렌더링하는 엔진에서는 이 검증이 렌더러 회귀만 막는다 — 모델의 발산은 애초에 성립하지 않으므로, 이 검증의 통과를 두 표현이 의미까지 일치한다는 근거로 읽어서는 안 된다.

## Related

- [ADR agent/0002: 가설 트리 라이프사이클](0002-hypothesis-tree-lifecycle.md) — 5 Whys 가드레일이 가설 생성·분기에도 적용됨
- [ADR agent/0006: 중단 조건 판단](0006-termination-conditions.md) — 중단 후 보고서 생성으로 전이
- [ADR agent/0008: 플레이북 생성](0008-playbook-generation.md) — 리포트에 포함되는 플레이북의 구조
- [ADR agent/0011: CC Headless 전문 서브 에이전트 오케스트레이션](0011-cc-headless-prompt-driven-rca.md) — CC Headless의 보고서 프롬프트에도 동일 가드레일 주입
- [ADR agent/0017: 플레이북 실행 에이전트](0017-playbook-execution-agent.md) — 리포트와 분리된 실행 생명주기
- [ADR agent/0018: 플레이북 회고](0018-playbook-retrospective.md) — 승인 화면이 표기하는 검증 상태를 승격시키는 단계
