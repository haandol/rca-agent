# ADR 0017: 플레이북 실행 에이전트 — 사용자 승인 게이트와 파괴적 액션 차단

Date: 2026-07-30

## Status

Accepted (2026-07-30)

## Context

RCA가 근본 원인을 찾고 대응 절차를 플레이북으로 남기더라도, 그 절차를 실행할
주체와 경계가 없으면 MTTR은 줄지 않는다. 이전 결정은 분석 결과가 확정되면 서버가
허용 목록에 있는 장애 리셋을 자동으로 호출하는 방식이었다. 이 방식에는 세 가지
한계가 있다.

첫째, **플레이북이 실행에서 배제된다.** 실행 대상은 검증 단계가 산출한 원인 유형과
서버 허용 목록으로만 결정되고, 플레이북의 절차는 사람이 읽는 참고 자료로만 남는다.
그 결과 플레이북의 정확성이 실행으로 검증되지 않고, 절차가 틀려도 아무도 모른다.

둘째, **실행 가능한 조치가 데모용 리셋 몇 종으로 고정된다.** 플레이북이 어떤 절차를
기술하더라도 그 절차를 수행할 수 없으므로, 플레이북의 표현력이 실행 능력을 초과한다.

셋째, **사람이 실행 시점을 통제할 수 없다.** 분석 직후 같은 실행 안에서 복구가
시작되므로, 운영자가 원인 판단에 동의하는지와 무관하게 쓰기 작업이 일어난다.
자동 복구를 기본 비활성으로 두는 것은 통제가 아니라 기능 자체의 회피다.

동시에 실행 권한을 넓히면 오판이 장애 확대로 이어질 수 있다. 넓힌 권한이 안전하려면
"무엇을 해도 되는가"가 아니라 "무엇은 절대 안 되는가"를 기준으로 경계를 세워야 한다.

## Decision Drivers

- 플레이북 절차가 실행의 근거여야 한다. 실행되지 않는 절차는 검증되지 않는다.
- 실행은 사람의 명시적 승인 없이 시작되어서는 안 된다.
- 실행 가능한 명령의 범위가 플레이북의 표현력을 제약해서는 안 된다.
- 되돌릴 수 없는 조치는 모델 판단과 무관하게 차단되어야 한다.
- 실행 실패가 이미 만들어진 분석 결과를 훼손해서는 안 된다.
- 실행 과정에서 발생한 명령 오류가 플레이북 개선에 쓰일 수 있어야 한다.

## Decision

플레이북 실행을 **분석과 분리된 별도 에이전트**가 담당하며, **사용자 승인
게이트**를 통과한 뒤에만 시작한다.

### 자동 복구 폐지와 승인 게이트

분석 파이프라인은 리포트와 그 안의 플레이북을 만든 뒤 종료한다. 확정 여부와
무관하게 어떤 쓰기 작업도 수행하지 않는다. 실행은 사람이 명시적으로 요청할 때만
시작하며, 승인 없이 시작된 실행은 존재할 수 없다.

승인 요청은 어느 분석 엔진이 만든 리포트인지와 무관하게 같은 실행 경로로 들어간다.
엔진별 실행 경로를 두지 않는다 — 안전 정책이 두 곳에 복제되면 시간이 지나며
갈라지고, 한쪽의 정책 회귀가 다른 쪽 검증으로 드러나지 않는다.

### 실행 근거는 플레이북

실행 에이전트는 승인된 리포트의 플레이북 절차를 근거로 명령을 수행한다. 원인 유형과
허용 목록의 매핑으로 대상을 정하지 않는다. 플레이북이 실행 근거가 되므로, 플레이북은
사람이 읽는 서술과 함께 실행에 필요한 구조화된 절차를 보유해야 한다
([ADR 0008](0008-playbook-generation.md)).

### 안전 경계 — 파괴적 액션 거부

대상 리소스는 제한하지 않는다. 대신 **되돌릴 수 없는 조치를 거부 목록으로 차단**한다.
경계를 허용 목록으로 세우면 플레이북이 기술할 수 있는 절차가 목록에 갇히므로, 실행
근거를 플레이북으로 옮긴 이 결정과 모순된다.

판정은 **제어 평면 작업 이름의 어휘**를 기준으로 한다. 다음 의미를 가진 작업은
거부한다.

- 리소스·인스턴스·클러스터의 삭제와 영구 종료
- 데이터·스냅샷·백업·아카이브의 삭제
- 자격 증명·정책·권한 부여의 회수와 삭제
- 계정·조직 수준의 구성 변경

다음은 허용한다.

- 모든 조회와 관측
- 재시작, 롤링 배포, 스케일 조정
- 구성 값의 변경과 되돌리기
- 검증 환경의 장애 리셋

**판정은 서버가 수행한다.** 실행 에이전트의 프롬프트 지시로 제한하지 않는다.
프롬프트만으로 세운 경계는 우회될 수 있으므로, 실행 도구가 명령을 파싱해 작업
이름을 추출하고 거부 어휘와 대조한 뒤 실행 여부를 결정한다. 작업 이름을 추출할 수
없는 명령은 판정할 수 없으므로 거부한다 — 판정 불가를 허용으로 해석하면 거부
목록이 무력화된다.

차단된 명령은 실행 증거에 차단 사실과 사유로 기록되고, 해당 절차는 수동 조치
필요로 남는다. 차단이 실행 전체를 중단시키지는 않는다.

### 실행 증거

플레이북의 정확성을 개선하려면 무엇이 어떻게 실패했는지가 명령 단위로 남아야 한다.
실행 증거는 각 시도마다 다음을 보유한다.

- 실행 중이던 플레이북 절차의 식별자
- 실행한 명령과 전달한 인자
- 종료 상태와 오류 출력
- 실패의 분류
- 재시도와 교정 내역
- 관측 결과

증거는 실행 단위로 누적되며 실행이 끝난 뒤에도 보존된다. 실패한 실행의 증거도
사람이 읽을 수 있어야 한다.

### 해결 판정과 완료

실행 에이전트는 절차 수행 후 이슈가 해소되었는지 관측하고, 그 결과로 실행 상태를
확정한다. 해소가 확인되면 완료로 전이한다. 해소되지 않았거나 관측으로 확정할 수
없으면 완료로 전이하지 않는다 — 관측 실패를 해결로 추정하면 미해결 장애가 완료로
기록된다.

### 생명주기 분리

분석 세션과 실행은 서로 다른 생명주기를 가진다. 실행 실패가 분석 세션을 실패로
만들지 않으며, 이미 저장된 리포트를 변경하지 않는다. 실행이 여러 번 시도되어도
분석 결과는 하나로 유지된다.

실행 상태의 허용 전이는 다음과 같다.

- 승인 대기 → 실행 중 (사용자 승인)
- 실행 중 → 검증 중 → 해결 / 미해결
- 실행 중 → 실패 / 취소

해결은 실행의 종료 상태이며 회고의 진입 조건이다
([ADR 0018](0018-playbook-retrospective.md)). 미해결과 실패는 회고로 진행하지
않는다.

### 멱등성

같은 승인 요청이 재전달되어도 실행이 중복되어서는 안 된다. 실행 요청은 안정적인
식별자를 가지며, 실행 주체는 claim으로 단일 실행을 보장한다. 외부 쓰기는 claim에
종속된 부작용 lease 안에서만 시작한다([ADR infra/0006](../infra/0006-session-recovery-on-restart.md)).

## 대안 검토

| 대안 | 장점 | 단점 및 미채택 이유 |
|------|------|---------------------|
| 확정 원인 + 허용 목록 기반 자동 복구 (이전 결정) | 사람 개입 없이 MTTR을 줄이고 실행 경계가 좁아 안전하다. | 플레이북이 실행에서 배제되어 절차의 정확성이 검증되지 않는다. 실행 가능 조치가 목록에 갇혀 플레이북 표현력을 제약하고, 운영자가 실행 시점을 통제할 수 없다. |
| 확정 원인은 자동, 미확정은 승인 | 확실한 경우의 속도와 불확실한 경우의 통제를 함께 얻는다. | 진입점이 둘이므로 안전 정책과 멱등성 로직을 양쪽에 유지해야 한다. 확정 판정 자체가 오판일 수 있어 자동 경로가 남는 한 통제가 성립하지 않는다. |
| 플레이북 근거 실행 + 항상 사용자 승인 + 파괴적 액션 거부 | 플레이북이 실행으로 검증되고, 실행 시점이 통제되며, 절차 표현력이 제약되지 않는다. | 사람이 승인해야 하므로 야간·무인 시간대의 MTTR 단축 효과가 없다. 거부 목록은 새로운 파괴적 작업 어휘가 등장하면 갱신이 필요하다. |
| 허용 목록으로 넓은 권한 관리 | 실행 가능한 것이 명시적이라 감사에 유리하다. | 플레이북이 기술할 수 있는 절차가 목록에 종속되어, 실행 근거를 플레이북으로 옮긴 결정과 모순된다. |

## Consequences

### Positive

- 플레이북이 실행 근거가 되므로 절차의 정확성이 실행으로 검증된다.
- 실행 시점이 사람의 통제 아래 있어 오판이 즉시 쓰기 작업으로 이어지지 않는다.
- 실행 가능한 명령이 플레이북 표현력을 제약하지 않는다.
- 실행 경로가 하나이므로 안전 정책을 한 곳에서 유지한다.
- 명령 단위 증거가 남아 플레이북 개선의 입력이 된다.
- 실행 실패가 분석 결과를 훼손하지 않는다.

### Negative

- 사람 승인이 필요하므로 무인 시간대의 자동 복구 효과가 사라진다.
- 실행 권한이 넓어져 거부 목록의 정확성이 안전의 단일 의존점이 된다.
- 실행 증거의 저장량이 리셋 결과 한 장보다 크게 늘어난다.
- 실행 상태가 분석 세션과 별도로 관리되어 조회 경로가 늘어난다.

### Risks

- 거부 어휘에 없는 파괴적 작업이 통과할 수 있다. 판정 불가 명령을 거부로
  처리하고 거부 어휘의 확장을 회귀 테스트로 고정한다.
- 명령 파싱을 우회하는 형태로 파괴적 작업이 전달될 수 있다. 셸 합성과 중첩 호출을
  판정 불가로 분류해 거부한다.
- 플레이북 절차가 틀리면 실행이 무의미한 명령을 수행할 수 있다. 파괴적 액션이 아닌
  범위에서는 되돌릴 수 있으므로, 회고가 절차를 교정하는 경로로 흡수한다.
- 관측으로 해결을 확정할 수 없는 장애는 완료로 전이하지 않아 미해결이 누적될 수
  있다. 완료 판정을 추정으로 대체하지 않고 사람이 증거를 읽는 경로를 유지한다.
- 승인 요청이 재전달되면 실행이 중복될 수 있다. 안정 식별자와 claim, 부작용
  lease로 단일 실행을 보장한다.

## Related

- [ADR 0008: 플레이북 생성](0008-playbook-generation.md) — 실행 근거가 되는 플레이북의 구조
- [ADR 0018: 플레이북 회고](0018-playbook-retrospective.md) — 실행 증거로 플레이북을 갱신하는 단계
- [ADR 0007: RCA 보고서 생성](0007-rca-report-generation.md) — 플레이북을 포함한 단일 리포트
- [ADR 0011: CC Headless 오케스트레이션](0011-cc-headless-prompt-driven-rca.md) — 분석 파이프라인에서 복구 단계 제거
- [ADR infra/0006: 세션 복구](../infra/0006-session-recovery-on-restart.md) — claim과 부작용 lease
- [ADR infra/0008: 플레이북 실행 인프라](../infra/0008-playbook-execution-stack.md) — 실행 에이전트의 배포와 트리거 경로
