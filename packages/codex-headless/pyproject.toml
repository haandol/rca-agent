[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-headless"
version = "0.1.1"
requires-python = ">=3.13"
dependencies = [
    "aws-bedrock-token-generator>=1.1.0",
    "boto3>=1.35",
    "fastmcp>=2.0",
    "structlog>=25.1",
]

[project.scripts]
codex-headless-eval = "codex_headless.eval_adapter:main"

[project.optional-dependencies]
dev = [
    "ruff>=0.11",
    "pytest>=8.0",
    "pytest-asyncio>=0.25",
    "moto[s3,dynamodb,sqs,sns]>=5.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N"]
ignore = ["B008"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
