{
  "name": "@org/cc-headless",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "docker build -t cc-headless .",
    "lint": "uv run ruff check src/ tests/",
    "lint:fix": "uv run ruff check --fix src/ tests/",
    "format": "uv run ruff format src/ tests/",
    "test": "uv run pytest tests/"
  }
}
